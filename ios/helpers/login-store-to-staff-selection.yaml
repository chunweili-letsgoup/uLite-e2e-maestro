appId: ${APP_ID}
name: "iOS Setup - Store Login to Staff Selection"
---
- launchApp:
    clearState: false
    stopApp: true
    permissions:
      notifications: deny
- tapOn:
    text: "(Don.t Allow|不允許)"
    optional: true
- extendedWaitUntil:
    visible: "Email (address|or phone number)"
    timeout: 15000
- tapOn:
    text: "Email (address|or phone number)"
    waitToSettleTimeoutMs: 300
- inputText: ${STORE_EMAIL}
- tapOn:
    text: "Password"
    index: 1
    waitToSettleTimeoutMs: 300
- inputText: ${STORE_PASSWORD}
- hideKeyboard
- tapOn:
    text: "Login|Log in"
    waitToSettleTimeoutMs: 500
- extendedWaitUntil:
    visible: ${STAFF_A}
    timeout: 15000
    label: "Store login routes to Staff Selection"
